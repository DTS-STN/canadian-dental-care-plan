# This file defines ingress resources that effectively put the frontend application in "maintenance mode".
#
# To enable maintenance mode, reference this file in kustomization.yaml.
# This will route public traffic to the maintenance service instead of the main application.
#
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend
  labels:
    app.kubernetes.io/name: frontend
spec:
  ingressClassName: nginx
  tls:
    # TLS secret `devdpdtsstncom` and `devdpinternaldtsstncom` are provisioned
    # out-of-band by the dev cluster platform (e.g., shared certificate management)
    # and are not managed in this repo.
    - secretName: devdpdtsstncom
      hosts:
        - cdcp-uat1.dev-dp.dts-stn.com
    - secretName: devdpinternaldtsstncom
      hosts:
        - cdcp-uat1.dev-dp-internal.dts-stn.com
  rules:
    - host: cdcp-uat1.dev-dp.dts-stn.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: maintenance
                port:
                  name: http
    - host: cdcp-uat1.dev-dp-internal.dts-stn.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  name: application